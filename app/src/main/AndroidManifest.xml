<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 🔑 Agrega aquí el permiso -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AppUx"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRUEBA"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".codigoverificacion"
            android:exported="false" />
        <activity
            android:name=".DarAltaCuenta"
            android:exported="false"
            android:theme="@style/Theme.PRUEBA" />
        <activity
            android:name=".ActivarCuenta"
            android:exported="false" />
        <activity
            android:name=".RecuperacionCuenta"
            android:exported="false" />
        <activity android:name=".RegistrarCuenta" />
        <activity
            android:name=".SegundaP"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>